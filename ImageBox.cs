using System.Windows.Forms;

namespace MazeBuilder
{
   public sealed class ImageBox : Panel
   {
      public ImageBox()
      {
         AutoScroll = true;
         DoubleBuffered = true;
      }
   }
}
